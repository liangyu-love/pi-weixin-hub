{
  "name": "pi-weixin-cli",
  "version": "0.3.0",
  "private": false,
  "type": "module",
  "description": "Weixin (WeChat) bridge for Pi Agent — bidirectional message relay via RPC with full UI interaction support",
  "keywords": [
    "pi",
    "weixin",
    "wechat",
    "agent",
    "rpc",
    "bridge",
    "bot"
  ],
  "bin": {
    "pi-weixin-cli": "dist/main.js"
  },
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "postbuild": "chmod +x dist/main.js",
    "prepack": "npm run build",
    "start": "node dist/main.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/liangyu-love/pi-weixin-hub.git"
  },
  "license": "MIT",
  "dependencies": {
    "qrcode-terminal": "^0.12.0"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
